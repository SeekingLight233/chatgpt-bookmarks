// import { bookmarkStore } from "~model/bookmark"
// import { mockBookmarks, mockCurSessionId } from "./mockData"

// describe('bookmark model tests', () => {
//   beforeEach(() => {
//     bookmarkStore.allBookmarks = mockBookmarks
//     bookmarkStore.curSessionId = mockCurSessionId
//   });
//   afterEach(() => {
//     bookmarkStore.allBookmarks = []
//   })

//   it("should init correctly", () => {
//     expect(bookmarkStore.allBookmarks.length).toBe(10)
//     expect(bookmarkStore.curSessionId).toBe(mockCurSessionId)
//   })
// })

describe("first", () => {
  it("sum", () => {
    expect(1 + 2).toBe(3)
  })
})
